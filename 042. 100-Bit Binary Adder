/*
Developer   - Sriram Venkata Krishna
Date        - 08-09-2025
Platform    - HDL Bits
*/

//042. 100-Bit Binary Adder

module top_module
    ( 
    	input [99:0] a, b,
    	input cin,
    	output [99:0] cout,
    	output [99:0] sum 
    );
    
    genvar i;
    
    assign sum[0] = (a[0] ^ b[0] ^ cin);
    assign cout[0] = (cin & (a[0] | b[0])) | (a[0] & b[0]);
    
    generate
        for (i = 1; i < 100; i = i + 1) begin : full_adder
            assign sum[i] = (a[i] ^ b[i] ^ cout[i-1]);
            assign cout[i] = (cout[i-1] & (a[i] | b[i])) | (a[i] & b[i]);
        end
    endgenerate

endmodule
